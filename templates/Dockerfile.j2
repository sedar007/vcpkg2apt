# Dockerfile generated by vcpkg-to-apt
# Source: {{ source_file }}
# Do not hand-edit the AUTO-GENERATED section if you plan to regenerate this
# file later — your edits will be overwritten.

FROM ubuntu:24.04
{%- set all_packages = base_packages + apt_packages %}
# --- AUTO-GENERATED: base packages + apt equivalents of vcpkg dependencies ---
RUN apt-get update && apt-get install -y --no-install-recommends \
{%- for pkg in all_packages %}
    {{ pkg }} \
{%- endfor %}
    && rm -rf /var/lib/apt/lists/*
# --- END AUTO-GENERATED ---

WORKDIR /app
COPY . .

RUN cmake -B build -G Ninja -DCMAKE_BUILD_TYPE=Release \
    && cmake --build build

CMD ["./build/{{ binary_name }}"]
